(function() {
  var add, herpderp, minus, subtract;

  add = function(a, b) {
    return a + b;
  };

  minus = function(a, b, c) {
    return a - b - c;
  };

  herpderp = function(d, e, f) {
    return console.log(d);
  };

  console.heym8y;

  subtract = function(a, b) {
    return a - b;
  };

  alert(add(7, subtract(4, 1)));

}).call(this);
